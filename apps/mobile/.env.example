# The mobile app reads the repo-root .env. Link it once:
#
#   ln -s ../../.env .env
#
# Then set EXPO_PUBLIC_API_URL there (see the root .env.example). Expo inlines
# EXPO_PUBLIC_* at bundle time — restart `expo start` after changing it.
