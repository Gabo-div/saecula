// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: timeline.sql

package gen

import (
	"context"
)

const timelineTexts = `-- name: TimelineTexts :many
SELECT DISTINCT ON (entity_id) entity_id, language_code, translation_id, raw_content, metadata
FROM text_documents
WHERE entity_id = ANY($1::text[])
  AND (($2::text <> '' AND translation_id = $2::text)
       OR ($2::text = '' AND language_code = $3::text))
ORDER BY entity_id, translation_id
`

type TimelineTextsParams struct {
	EntityIds     []string
	TranslationID string
	Lang          string
}

func (q *Queries) TimelineTexts(ctx context.Context, arg TimelineTextsParams) ([]TextDocument, error) {
	rows, err := q.db.Query(ctx, timelineTexts, arg.EntityIds, arg.TranslationID, arg.Lang)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TextDocument
	for rows.Next() {
		var i TextDocument
		if err := rows.Scan(
			&i.EntityID,
			&i.LanguageCode,
			&i.TranslationID,
			&i.RawContent,
			&i.Metadata,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
