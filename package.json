{
  "name": "saecula",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "migrate": "bun --filter @saecula/back run migrate",
    "e2e": "bash scripts/e2e.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.9.0"
  },
  "packageManager": "bun@1.3.14"
}
